import React from "react";
import { Alert, Box } from "@mui/material";
import InfoOutlinedIcon from "@mui/icons-material/InfoOutlined";

const InfoBanner = ({ children }) => (
  <Box sx={{ my: 0, mb: 3, mx: "auto", maxWidth: 900 }}>
    <Alert
      icon={<InfoOutlinedIcon fontSize="inherit" />}
      severity="info"
      sx={{
        fontSize: "1.07rem",
        alignItems: "center",
        boxShadow: 1,
        bgcolor: "#e7f3fe",
        color: "#15395b",
        border: "1.5px solid #90caf9",
        borderRadius: 2,
      }}
    >
      {children || (
        <>
          <strong>Reminder:</strong> After saving your data to the database, please note that the Looker dashboard will reflect your updates within <b>3–4 hours</b>.
        </>
      )}
    </Alert>
  </Box>
);

export default InfoBanner;
