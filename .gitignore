        {!showTip && (
          <Tooltip title="Show tip">
            <IconButton
              size="small"
              onClick={() => setShowTip(true)}
              sx={{ color: "#1976d2", ml: 0.5 }}
            >
              <InfoOutlinedIcon fontSize="small" />
            </IconButton>
          </Tooltip>
        )}
