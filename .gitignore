    {confirmBU ? (
      <>Are you sure you want to accept the Auditor’s recommendations for <strong>{confirmBU.subcategoryName}</strong> and <strong>{confirmBU.businessUnitAbbr}</strong>?</>
    ) : (
      'Are you sure you want to accept the Auditor’s recommendations?'
    )}
