DECLARE @Today DATE = GETDATE(); -- or use a parameter
DECLARE @Quarter INT = DATEPART(QUARTER, @Today);
DECLARE @Year INT = YEAR(@Today);

-- Final label
DECLARE @ReportQuarter NVARCHAR(20) = CONCAT(@Year, ' Q', @Quarter);

SELECT @ReportQuarter AS ReportQuarter;
